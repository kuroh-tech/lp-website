# 初回インポート手順

1. ルートに `xserver-db-config.php` を配置して DB 情報を入力
2. 次へアクセス
   - `/_migration/import.php?token=ac240516a587833b3c5ef44d2bb73471`
3. 完了後にサイト表示を確認
4. 問題なければ `_migration/` を削除

このディレクトリには SQL ダンプと補助スクリプトが含まれます。
